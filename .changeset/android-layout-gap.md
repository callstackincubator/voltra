---
'@use-voltra/android-client': minor
'@use-voltra/android': patch
---

The `gap` style now spaces children apart on Android for `Column`, `Row`, `LazyColumn`, and `LazyVerticalGrid`, matching the existing iOS behavior.
