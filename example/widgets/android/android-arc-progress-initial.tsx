import { AndroidArcProgressWidget } from './AndroidArcProgressWidget'

const initialState = [
  {
    size: { width: 250, height: 250 },
    content: <AndroidArcProgressWidget />,
  },
]

export default initialState
